name: smart_campus_pro
description: Smart Campus Notice Board
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^1.1.0
  shared_preferences: ^2.2.3        # for token storage
  image_picker: ^1.0.4              # for image upload
  intl: ^0.19.0                     # for date formatting
  flutter_secure_storage: ^9.0.0    # secure token storage (optional but recommended)
  # For offline caching (optional later)
  # hive_flutter: ^1.1.0